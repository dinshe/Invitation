# 🎵 Background Music Folder

Place your wedding background music file here.

Default expected file name: `wedding-music.mp3`

Supported formats: `.mp3`, `.m4a`, `.ogg`, `.wav`

If you use a different file name, update `musicFile` in `src/config.js`:
```js
musicFile: "./music/your-music-file.mp3"
```
